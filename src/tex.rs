use std::io::Error;

use repl::Item;

pub fn compose_tex(Items: Vec<Item>) -> Result<String, Error> {
    unimplemented!();
}

pub fn compile_tex(tex: String) -> Result<(), Error> {
    unimplemented!();
}